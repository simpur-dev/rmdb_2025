# RMDB 开发进度文档

> 本文件是项目的**开发状态快照**，用于快速回答三个问题：
> - 系统当前长什么样？（架构图）
> - 每个模块做到什么程度了？（进度矩阵）
> - 下一次开发从哪里接上？（切入点）
>
> 总体路线图请看 `db_plan.md`。

---

## 一、系统架构图

### 1.1 整体分层

```
┌─────────────────────────────────────────────────────────────┐
│                     SQL Client / CLI                         │
└─────────────────────────────────────────────────────────────┘
                             │
┌────────────────────────────┴────────────────────────────────┐
│  Parser (Lex/Yacc)  →  Analyze  →  Optimizer  →  Planner    │  ← 查询处理层
└────────────────────────────┬────────────────────────────────┘
                             │  PhysicalPlan
┌────────────────────────────┴────────────────────────────────┐
│                    Execution Engine (火山模型)                │  ← 执行引擎层
│                                                              │
│   ┌─────────────────────────────────────────────────────┐   │
│   │ AbstractExecutor: beginTuple/nextTuple/Next/is_end  │   │
│   └─────────────────────────────────────────────────────┘   │
│   SeqScan  IndexScan  Insert  Delete  Update                 │
│   NestedLoopJoin  Projection  Sort                             │
└────────────────────────────┬────────────────────────────────┘
                             │
┌────────────────────────────┴────────────────────────────────┐
│              System Catalog (SmManager / DbMeta)            │  ← 元数据层
│   TabMeta · ColMeta · IndexMeta · fhs_ · ihs_               │
└────────────────────────────┬────────────────────────────────┘
                             │
       ┌─────────────────────┴─────────────────────┐
       │                                            │
┌──────┴──────────┐                    ┌───────────┴────────┐
│  Record Manager │                    │   Index (B+ Tree)  │   ← 存取方法层
│  RmFileHandle   │                    │   IxIndexHandle    │
│  RmScan         │                    │   IxScan           │
└──────┬──────────┘                    └───────────┬────────┘
       │                                            │
       └─────────────────────┬─────────────────────┘
                             │
┌────────────────────────────┴────────────────────────────────┐
│            BufferPoolManager + LRU Replacer                 │  ← 缓冲池层
└────────────────────────────┬────────────────────────────────┘
                             │
┌────────────────────────────┴────────────────────────────────┐
│                     DiskManager (page I/O)                  │  ← 存储层
└─────────────────────────────────────────────────────────────┘

           ┌─────────────────────┐       ┌──────────────────┐
           │ TransactionManager  │       │   LogManager     │  ← 横切关注点
           │   (未完成)           │       │    (未完成)       │
           └─────────────────────┘       └──────────────────┘
```

### 1.2 执行器依赖关系

```
                   ┌────────────────────┐
                   │   QlManager        │  (执行入口)
                   └──────────┬─────────┘
                              │
              ┌───────────────┴───────────────┐
              │                               │
     ┌────────┴────────┐            ┌─────────┴─────────┐
     │  DML Executors  │            │  Query Executors  │
     └────────┬────────┘            └─────────┬─────────┘
              │                               │
  ┌───────────┼───────────┐      ┌────────────┼────────────┐
  │           │           │      │            │            │
Insert     Delete      Update  SeqScan    IndexScan    NestedJoin
  │           │           │      │            │            │
  └───────────┴─────┬─────┘      └────────────┼────────────┘
                    │                         │
              RmFileHandle              [Projection]  [Sort]
              (+ 索引维护)                     │
                                              ▼
                                         子算子链（火山模型）
```

### 1.3 调用链示例：`SELECT * FROM t WHERE id = 1`

```
Parser → AST → Analyze → LogicalPlan
                           │
                           ▼
                       Optimizer（选择 IndexScan）
                           │
                           ▼
                   IndexScanExecutor::beginTuple()
                           │
                           ▼
                   IxIndexHandle::lower_bound(key)
                           │
                           ▼
                   BufferPoolManager::fetch_page(leaf_page)
                           │
                           ▼
                   DiskManager::read_page(...)
```

---

## 二、模块完成度矩阵

### 图例：✅ 完成  🟡 部分完成  ❌ 未实现  🧪 有单元测试

| 层 | 模块 | 状态 | 测试 | 备注 |
|---|---|:---:|:---:|---|
| **存储层** | `DiskManager` | ✅ | 🧪 | 线程安全 + 重试 |
| 缓冲池 | `LRU Replacer` | ✅ | 🧪 | O(1) |
| 缓冲池 | `BufferPoolManager` | ✅ | 🧪 | 修复 delete_page / flush_page |
| 页面 | `Page shared latch` | ✅ | — | shared_mutex |
| **记录层** | `RmFileHandle` | ✅ | 🧪 | slotted page |
| | `RmScan` | ✅ | 🧪 | |
| **索引层** | B+ 树节点操作 | ✅ | 🧪 | |
| | 插入 / 分裂 | ✅ | 🧪 | |
| | 删除 / 合并 / 再分配 | ✅ | 🧪 | |
| | `lower_bound / upper_bound` | ✅ | 🧪 | |
| | `IxScan` | ✅ | 🧪 | shared latch 遍历 |
| | 并发控制（latch-coupling） | ✅ | — | |
| **执行层** | `SeqScanExecutor` | ✅ | 🧪 | |
| | `IndexScanExecutor` | ✅ | 🧪 | 支持范围条件 |
| | `InsertExecutor` | ✅ | 🧪/E2E | 含索引同步；**Step 2：pre-check → 真写入两段式，唯一索引冲突安全拦截** |
| | `DeleteExecutor` | ✅ | 🧪 | 含索引同步 |
| | `UpdateExecutor` | ✅ | E2E | pre-check + 自身豁免；唯一索引冲突 → failure |
| | `NestedLoopJoinExecutor` | ✅ | 🧪 | 缓存记录模式 |
| | `ProjectionExecutor` | ✅ | 🧪 | 按 sel_idxs_ 抽列拼接 |
| | `SortExecutor` | ✅ | 🧪 | 物化+索引排序 |
| **系统目录** | `create_db / drop_db` | ✅ | 🧪 | |
| | `create_table / desc_table / show_tables` | ✅ | 🧪 | |
| | `flush_meta` | ✅ | 🧪 | |
| | `open_db` | ✅ | 🧪 | 读取 DbMeta 并重建 fhs_/ihs_ |
| | `close_db` | ✅ | 🧪 | flush_meta + 清理句柄 + chdir .. |
| | `drop_table` | ✅ | 🧪 | 级联删索引 |
| | `create_index / drop_index` | ✅ | 🧪 | 含已有数据回填；两个重载 |
| **查询处理** | Parser (Lex/Yacc) | ✅ | — | 框架提供 |
| | Analyze | ✅ | E2E | SelectStmt 表存在校验、UpdateStmt/DeleteStmt/InsertStmt 分支全补、WHERE/SET/INSERT 均 INT→FLOAT 隐式提升、指定表名时列存在校验、CHAR 溢出检查均完成 |
| | Optimizer / Planner | ✅ | E2E | `get_index_cols` 改造：单列索引支持范围查询，多列索引仅 EQ |
| **事务** | `TransactionManager::begin/commit/abort` | 🟡 | E2E | 最小实现（无 MVCC/WAL/回滚），支持服务端跑通 DDL/DML |
| | `TransactionManager` MVCC | ❌ | — | WriteRecord 回滚、版本链未实现 |
| | `LogManager / Recovery` | ❌ | — | ARIES 未实现 |

---

## 三、Git 里程碑时间线

```
3910b4b  2026-04-12 13:32  初始导入（比赛骨架）
  │
  ├─ 阶段一：存储 & 缓冲池
  │   7336ed1  DiskManager 重试 + 线程安全
  │   a750e8c  LRU Replacer
  │   89b216b  BufferPoolManager
  │
  ├─ 阶段二：索引 B+ 树
  │   341c7bc  节点操作 insert/remove
  │   2db5f48  split
  │   6c29108  insert_into_parent / entry
  │   25c4eb3  删除路径 coalesce/redistribute
  │   4698386  lower/upper_bound + bug 修复
  │   63f0ae7  并发控制 latch-coupling
  │   81b52c4  IxScan shared latch
  │   b2c8240  Page shared_mutex
  │
  ├─ 阶段三：Record Manager
  │   105d44e  RmFileHandle / RmScan
  │
  ├─ 阶段四：测试与深度修复
  │   83a743e  B+ 树深度测试 + 空树/跨页 bug 修复
  │
  ├─ 阶段五：执行器扫描
  │   bce1e13  SeqScan / IndexScan 初版
  │   c7cb719  修复 get_col_offset + 19 单元测试
  │
  ├─ 阶段六：DML / Join
  │   7910279  DeleteExecutor
  │   cf0b103  NestedLoopJoin / Update 初版
  │
  ├─ 阶段七：Bug 修复
  │   85b6e42  delete/update/join 编译错误修复 + 开发文档重写
  │
  ├─ 阶段八：Projection / Sort
  │   30e19fa  实现 ProjectionExecutor、SortExecutor；新增执行层架构文档
  │
  ├─ 阶段九：Bug 修复 + 测试扩展
  │   buffer_pool_manager.cpp  修复 delete_page (unpin→pin) + flush_page 规范
  │   executor_update.h        修复 init_raw 重复调用 assert 崩溃
  │   executor_full_test.cpp   40 个执行器单元测试
  │   integration_test.cpp     26 个三层联动集成测试
  │
  ├─ 阶段十：文档 & 知识沉淀
  │   SYSTEM_LAYER.md / STORAGE_LAYER.md  架构图 + 流程图
  │
  ├─ 阶段十一：SmManager DDL 实现（2026-04-18 凌晨）
  │   5ec701a  docs: 新增存储层与系统目录层架构文档
  │   63db68f  feat(sm): 实现 open_db / close_db / drop_table
  │   1a938bc  feat(sm): 实现 create_index / drop_index
  │   3655699  test(sm): 20 个 DDL 综合测试 + create_index 回填 bug 修复
  │
  ├─ 阶段十二：TransactionManager 最小实现（2026-04-18 中午）
  │   3005dfa  feat(txn): 实现部分 TransactionManager::begin/commit/abort
  │   · 端到端验证：题目二测试点 1（建表/删表/show tables）完全通过
  │
  ├─ 阶段十三：Analyze 补全 + 类型提升（2026-04-18 下午）
  │   be065f5  feat(analyze): 实现 UpdateStmt 分支 + WHERE/SET 的 INT→FLOAT 隐式提升
  │   · learning_docs/AST_GUIDE.md  【新增】10 章 AST 学习文档
  │   · 端到端验证：题目二测试点 1/2/3 全部通过
  │
  ├─ 阶段十四：Analyze 校验全面完善（2026-04-18 晚间）
  │   · analyze.cpp  InsertStmt 完整重写：表存在 / 值数==列数 / 类型匹配 / INT→FLOAT 提升 / CHAR 溢出
  │   · analyze.cpp  UpdateStmt · DeleteStmt 顶部加 is_table() 显式校验
  │   · analyze.cpp  check_column 补全指定表名时的列存在性校验
  │   · 端到端验证：5 种 INSERT 错误全部被拦下（服务端稳定），题目二回归测试继续全通
  │
  ├─ 阶段十五：题目三 唯一索引 — Step 1 & 2（2026-04-18 傍晚）
      · errors.h         新增 IndexEntryDuplicateError（继承 RMDBError，自动 failure 输出）
      · executor_insert.h InsertExecutor::Next 重写：pre-check → 真写入两段式
                           - 所有唯一索引 get_value 查重后才 insert_record
                           - 冲突抛 IndexEntryDuplicateError，零副作用
                           - new/delete[] → unique_ptr<char[]>，修复内存泄漏
                           - 修复变量遮蔽（外层 i / 内层 k）
      · learning_docs/Q2_ANALYZE_GUIDE.md  【新增】10 章题目二复习文档
      · 端到端验证：唯一索引重复 key → output.txt 写 failure，服务端稳定，表数据干净
  │
  └─ 阶段十六：题目三 唯一索引完善 + Planner 改造（2026-04-19 晚）
      · executor_update.h  UpdateExecutor::Next 重写：
                           - 旧 key / 新 key 构造 → memcmp 判断是否变化
                           - 自身豁免：key 相同跳过、key 变化时排除自身 RID
                           - pre-check 全部通过 → delete_entry → update_record → insert_entry 三阶段原子提交
      · planner.cpp        get_index_cols 改造：
                           - 单列索引：任意比较符（=, <, >, <=, >=）均可走 IndexScan
                           - 多列索引：仅全 OP_EQ 时走 IndexScan（避免 B+ 树越界）
                           - 返回索引列名按索引定义顺序（修复列序不匹配）
      · 端到端验证：
        - 测试点 1（CREATE/DROP INDEX）：✅ 通过
        - 测试点 2（索引查询 7 条 SQL）：✅ 全部通过
        - 测试点 3（索引维护 + 唯一性 failure）：✅ 数据与 failure 位置全对
        - 测试点 4/5（性能对比）：🟡 待调优，索引加速比尚不满足 ≤70% 阈值
```

---

## 四、当前未提交改动

**状态**：✅ 已通过编译；**端到端：题目二 1/2/3 全通 + 题目三测试点 1/2/3 通过**。

### 测试矩阵

| 测试套件 | 用例数 | 覆盖模块 |
|---|:---:|---|
| `unit_test` | 5 | Legacy BPM / LRU / Storage |
| `record_index_test` | 20 | RM + B+ Tree 基础 |
| `record_index_deep_test` | 27 | RM + B+ Tree 深度（边界/并发） |
| `executor_scan_test` | 19 | SeqScan / IndexScan |
| `executor_full_test` | 40 | Insert/Delete/Update/Join/Projection/Sort |
| `integration_test` | 26 | Storage+Record+Index+Executor 三层联动 |
| **`sm_manager_test`** | **20** | **System Catalog DDL（新增）** |
| **合计** | **157** | 存储 / 记录 / 索引 / 执行 / 系统目录 全覆盖 |

### 本批改动

| 文件 | 修改摘要 |
|---|---|
| `errors.h` | **新增 `IndexEntryDuplicateError`**（继承 `RMDBError`） |
| `execution/executor_insert.h` | **`Next()` pre-check → 真写入两段式**，唯一索引查重 |
| `execution/executor_update.h` | **`Next()` 重写**：pre-check + 自身豁免 + 三阶段原子提交 |
| `optimizer/planner.cpp` | **`get_index_cols` 改造**：单列索引支持范围查询，多列索引仅 EQ |
| `learning_docs/Q2_ANALYZE_GUIDE.md` | 10 章题目二复习文档 |

---

## 五、下一步工作切入点

### ✅ 已完成
- 存储/缓冲池/记录/B+ 树索引/执行引擎/系统目录 DDL 全部实现并通过 157 单元测试
- **TransactionManager 最小版**：服务端可稳定接受多条 SQL，题目二测试点 1/2/3 端到端通过
- **Analyze 模块全面收尾**：四语句分支 + 表/列校验 + INT→FLOAT 提升 + CHAR 溢出
- **题目三 Step 1/2**：`IndexEntryDuplicateError` + `InsertExecutor` pre-check 两段式，重复 key 端到端验证通过
- 测试驱动发现并修复 5 个 Bug（BPM ×2、UpdateExecutor、SmManager create_index 回填、TransactionManager 空壳导致服务端段错）

### 🎯 P1：题目三 唯一索引 — 剩余步骤
- ~~Step 3：`UpdateExecutor` 改造~~ ✅ 完成
- **Step 5**：`SHOW INDEX` 全链路（parser / ast / analyze / optimizer / executor）
- **Step 6**：性能调优 — 测试点 4/5 索引加速比需 ≤70%

### 🎯 P2：查询处理层进一步完善
- `Analyze` — 反向 INT→FLOAT 提升（`WHERE int_col > 1.5` 场景）、JOIN jointree 规范化

### 🎯 P3：事务与恢复完整实现
- `TransactionManager` 补全 WriteRecord 回滚逻辑
- `LogManager` WAL 写入 + 刷盘
- ARIES Recovery（Analysis / Redo / Undo）

详见 `db_plan.md`。

---

## 六、下次开发 checklist

开项目时请按顺序确认：

1. 读本文「四、当前未提交改动」→ 若有残留先收尾
2. `git log -n 5` 看最近提交是否与本文一致
3. `git status` 查看工作区状态
4. 决定本次目标（补空壳 / 补测试 / 推新阶段）

---

## 七、开发记录模板

每次开发结束追加一段：

```md
## 开发记录 - YYYY-MM-DD

### 本次目标
- 

### 修改文件
- 

### 完成内容
- 

### 遗留问题
- 

### 下次从这里继续
- 
```

---

## 八、一句话当前状态

**题目三唯一索引核心功能完成：Insert/Update pre-check 两段式 + 自身豁免 + Planner 单列范围索引扫描。端到端：题目三测试点 1/2/3 通过；测试点 4/5 性能调优中。剩余：`SHOW INDEX` 全链路 + 索引加速比优化。**
